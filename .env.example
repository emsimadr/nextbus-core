# Secrets and environment overrides for bus-tracker.
# Copy to .env and edit. This file is gitignored -- never commit .env.

# REQUIRED: Your MBTA API key (get one free at https://api-v3.mbta.com/)
# Without it, rate limit is only 20 requests/min.
MBTA_API_KEY=your-key-here

# OPTIONAL: Protect your bus-tracker API with a key.
# If set, all /v1/* requests must include X-API-Key header.
# API_KEY=some-secret

# Server settings
PORT=8080
LOG_LEVEL=info
# CONFIG_PATH=/app/config.yaml
